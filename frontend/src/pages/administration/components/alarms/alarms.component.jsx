import React from 'react';
import strings from './../../../../constants/strings'
import "./alarms.component.css"

export default function Alarms(props) {
    return <h1 className="content-title">{strings.adminstration.alarms.title}</h1>
}